# metareview
- **metareview** (`skills/metareview/SKILL.md`) - mid-session quality audit
When the user types `/metareview`, invoke the Skill tool with `skill: "metareview"` before doing anything else.
